
namespace ET.Client
{
    /*
    [Callback(CallbackType.SessionStreamDispatcherServerInner)]
    public class SessionStreamDispatcherServerInner : IAction<Entity, UIEventContext>
    {
        public void Handle(Entity entity, UIEventContext uIEventContext)
        {
            //if (entity is xxxComponent)
            //执行预定义事件
        }
    }

    [Callback(UICallbackType.ScreenSizeChanged)]
    public class UIEventHandle_ScreenSizeChanged : IAction<int, int>
    {
        public void Handle(int w, int h)
        {
            //if (entity is xxxComponent)
            //执行预定义事件
            //Log.Debug($"UI相机分辨率变化  {args.screenWidth} {args.screenHeight} Stage.inst.HandleScreenSizeChanged");
            //await ETTask.CompletedTask;
        }
    }
    */
}
